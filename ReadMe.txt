To Run the Program make sure you have node installed.

Once you are in the directory just run command: node index.js

Input Format: boardingCard.json file is read as input, so array of boarding cards can be provided in boardingCard.json
Output Format: list of instructions is printed out on the console

////
BoardingCardModel.js represents the BoardingCardModel which can be scaled if needed
